///|
enum VNode[Msg] {
  Text(String)
  Element(String, Array[Attr[Msg]], Array[VNode[Msg]])
  Component(ComponentNode)
  Keyed(String, VNode[Msg])
}

///|
pub fn[A, B] VNode::map(self : VNode[A], f : (A) -> B) -> VNode[B] {
  match self {
    Text(s) => Text(s)
    Element(tag, attrs, children) => {
      let new_attrs = attrs.map(fn(a) { a.map(f) })
      let new_children = children.map(fn(c) { c.map(f) })
      Element(tag, new_attrs, new_children)
    }
    Component(comp) => Component(comp)
    Keyed(k, child) => Keyed(k, child.map(f))
  }
}

///|
pub fn[Msg] text(s : String) -> VNode[Msg] {
  Text(s)
}

///|
/// Convert a typed VNode tree to VNode[Unit] by binding all Event handlers
/// through a dispatch function, turning them into BoundEvent closures.
fn[CMsg] bind_events(
  vnode : VNode[CMsg],
  dispatch : (CMsg) -> Unit,
) -> VNode[Unit] {
  match vnode {
    Text(s) => Text(s)
    Element(tag, attrs, children) => {
      let new_attrs = attrs.map(fn(a) { bind_attr(a, dispatch) })
      let new_children = children.map(fn(c) { bind_events(c, dispatch) })
      Element(tag, new_attrs, new_children)
    }
    Component(comp) => Component(comp)
    Keyed(k, child) => Keyed(k, bind_events(child, dispatch))
  }
}

///|
pub fn[Msg] keyed(key : String, child : VNode[Msg]) -> VNode[Msg] {
  Keyed(key, child)
}

///|
/// Wrap an array of (key, vnode) pairs into Keyed vnodes for use as
/// children of any container element: `ul([], keyed_list(items))`
pub fn[Msg] keyed_list(
  items : Array[(String, VNode[Msg])],
) -> Array[VNode[Msg]] {
  items.map(fn(pair) { Keyed(pair.0, pair.1) })
}

///|
pub fn[Msg] el(
  tag : String,
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  Element(tag, attrs, children)
}

///|
// BEGIN GENERATED TAG HELPERS

// Generated by scripts/generate-vnode-tags.mjs. Do not edit manually.

///|
pub fn[Msg] div(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("div", attrs, children)
}

///|
pub fn[Msg] span(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("span", attrs, children)
}

///|
pub fn[Msg] button(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("button", attrs, children)
}

///|
pub fn[Msg] h1(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("h1", attrs, children)
}

///|
pub fn[Msg] h2(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("h2", attrs, children)
}

///|
pub fn[Msg] h3(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("h3", attrs, children)
}

///|
pub fn[Msg] p(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("p", attrs, children)
}

///|
pub fn[Msg] ul(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("ul", attrs, children)
}

///|
pub fn[Msg] li(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("li", attrs, children)
}

///|
pub fn[Msg] label(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("label", attrs, children)
}

///|
pub fn[Msg] section(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("section", attrs, children)
}

///|
pub fn[Msg] header(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("header", attrs, children)
}

///|
pub fn[Msg] footer(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("footer", attrs, children)
}

///|
pub fn[Msg] a(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("a", attrs, children)
}

///|
pub fn[Msg] form(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("form", attrs, children)
}

///|
pub fn[Msg] textarea(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("textarea", attrs, children)
}

///|
pub fn[Msg] select(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("select", attrs, children)
}

///|
pub fn[Msg] option(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("option", attrs, children)
}

///|
pub fn[Msg] table(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("table", attrs, children)
}

///|
pub fn[Msg] tr(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("tr", attrs, children)
}

///|
pub fn[Msg] td(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("td", attrs, children)
}

///|
pub fn[Msg] th(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("th", attrs, children)
}

///|
pub fn[Msg] nav(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("nav", attrs, children)
}

///|
pub fn[Msg] pre(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("pre", attrs, children)
}

///|
pub fn[Msg] code(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("code", attrs, children)
}

///|
pub fn[Msg] em(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("em", attrs, children)
}

///|
pub fn[Msg] strong(
  attrs : Array[Attr[Msg]],
  children : Array[VNode[Msg]],
) -> VNode[Msg] {
  el("strong", attrs, children)
}

///|
pub fn[Msg] input(attrs : Array[Attr[Msg]]) -> VNode[Msg] {
  el("input", attrs, [])
}

///|
pub fn[Msg] img(attrs : Array[Attr[Msg]]) -> VNode[Msg] {
  el("img", attrs, [])
}

///|
pub fn[Msg] br(attrs : Array[Attr[Msg]]) -> VNode[Msg] {
  el("br", attrs, [])
}

///|
pub fn[Msg] hr(attrs : Array[Attr[Msg]]) -> VNode[Msg] {
  el("hr", attrs, [])
}

// END GENERATED TAG HELPERS
