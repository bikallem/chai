{
  "name": "chai-tests",
  "private": true,
  "scripts": {
    "test": "cd .. && bash ./scripts/build-example-assets.sh && cd tests && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
