{
  "name": "chai-tests",
  "private": true,
  "scripts": {
    "test": "moon build --target js --release && cd ../tests && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}