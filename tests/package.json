{
  "name": "mvdom-tests",
  "private": true,
  "scripts": {
    "test": "cd ../examples && moon build --target js --release && cd ../tests && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
